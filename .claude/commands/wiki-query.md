Answer this question using the andromeda-wiki: $ARGUMENTS

## How to answer

1. Read `andromeda-wiki/index.md`. Identify which entity, concept, or interpreter pages are relevant.
2. Read those pages. Answer from the wiki with inline citations (e.g., `[[entities/view]]`, `[6-1-VIEW.html]`).
3. Close your answer with the confidence tier: **triangulated** (spec + ref impl + andromeda code), **code-to-spec** (spec + one ref impl), **spec-only** (spec only, no code cross-check), or **unverified** (tagged `(agidev, unverified)` in the wiki).

If the wiki is silent on the question: fall back to the relevant `AGI_Specifications/Specifications/` chapter for that specific fact only. Note which chapter you used and whether it corroborates or contradicts what the wiki does say. Offer to file the answer with `/wiki-file` if it adds durable value.

If the claim is marked `(agidev, unverified)` and the user needs high confidence: recommend a ScummVM cross-check (`engines/agi/`) as the next step.
