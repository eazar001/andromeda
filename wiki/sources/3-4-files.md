# Source: 3-4-Files.html

Peter Kelly's AGI Specifications, vendored at `AGI_Specifications/Specifications/3-4-Files.html`. Titled "3.4 Sample Code" — a reference table of historical AGI decoder implementations (Lance Ewing's `agifiles.c`/`agifiles.h`/`general.h`/`volx2.c`/`xv3.pas`, Jeremy Hayes's `agiver.pas`).

## Scope

Bibliographic only. No file-format content — the chapter is a table of public-domain C and Pascal decoders that demonstrate the formats already specified in [[sources/3-1-files]], [[sources/3-2-files]], and [[sources/3-3-files]].

## Informs

Nothing new. All formats referenced here are already covered by the prior Files-group source pages.

## Notes

- Despite the `*-Files.html` filename, this chapter is conceptually closer to [[sources/8-3-samplecode]] (the explicit "Sample Code" chapter in Group 7 — to be ingested later). No content overlap with `3-1`–`3-3`, so it stands alone as a chapter-marker.
- No conflicts; nothing to validate against `resource/`.
